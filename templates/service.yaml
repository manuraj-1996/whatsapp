apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.metadata.service.app }}
  
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Values.metadata.service.app }}

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      
